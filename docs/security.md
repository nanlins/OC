# OC 安全模�?

> 说�? OC ?��??�架?��?guard ?��??�容?��?箱、Web 安全?��??�管?�、�?件系统防?�、审?��???
## 1. guard fail-closed ?��?

?�?�特?�动作�? `guard(action, input) ??allow | hold | deny` ?��?�?
- **?�铸?�值�?�?DENY**（`WeakSet` ?��?检??+ 运�??��?底�?
- **decide ?��? ??DENY**（fail-closed�?- **grant ?�满�?hold，永不松??deny**
- 审批?�放?��??��?结�?检??+ ?��?验活行�?`pending_approvals` 存在）�? ?�好?��?一�?
**?�件**：`src/guard/guard.ts`?�`src/guard/guard-actions.ts`?�`src/delivery-guard.ts`

## 2. 容器沙箱

- **`--cap-drop=ALL`**：丢弃全??Linux capabilities
- **`--security-opt=no-new-privileges`**：�?止�?进�??��?
- **`--init`**：tini 作为 PID 1，正确转??SIGTERM
- **?�件系�??�离**：只?�载?��?路�?（`/workspace`?�`/workspace/agent`?�`inbound.db`(RO)?�`outbound.db`�?- **?�载?��???*：`~/.config/OC/mount-allowlist.json`，在项目?��?外�?容器�?agent 不可达�?
- **资�??�制**：`--cpus`?�`--memory`?�`--pids-limit`（floor + finite ?��?�?- **?�口网�?**：可??`--internal` 网�?（egress lockdown�?
## 3. Web 安全

| ?�御 | 实现 |
|------|------|
| **fail-closed ?��?** | `WEB_TOKEN` ?��?置时?�动?��??�机 token ?��??��?`data/web-token`）�??��?�?token |
| **CSRF** | POST 请�??��? `Sec-Fetch-Site: cross-site` �?Origin?�Host |
| **路�?穿�?** | `/api/traces/:id` �?`isSafeTraceId`（�??��?�?+ resolve 容纳?��?�?|
| **请�?体�???* | �?1MB ?�止累积，�???413 |
| **常�??�间 token 比�?** | `timingSafeEqual` |
| **SQL 注入** | ?�部?�数?�查询�??�态�??��??��??�校验�?`COL_RE` �??�?|

**?�件**：`src/web/api.ts`?�`src/web/server.ts`?�`src/eval/trace.ts`

## 4. 密钥管�?

- **不�? process.env**：`.env` �?`readEnvFile` ?��??�读?��??��?不�???`process.env`
- **不�? docker argv**：�??��? 0600 临时?�件，�? `--env-file` 注入容器，容?�退?�即??- **不�? Git**：`.env` �?gitignore
- **诚�??��?**：弱于基�?OneCLI 网关??token 不�?容器"（docs/architecture.md §7 已记录�?

**?�件**：`src/env.ts`?�`src/providers/openai.ts`?�`src/container-runner.ts`

## 5. ?�件与�?件系统�???
?��??�御（`src/attachment-safety.ts`?�`src/inbox-safety.ts`）�?

1. `basename` ?��??�—�?绝含路�??��?符�??�件??2. `lstat` ?��? symlink ?��??��?
3. `realpath` 容纳（解?�符?�链?��??��?实路径�?
4. `wx` ?��??�——�?绝�??�已?��?�?
?�载安全（`src/modules/mount-security.ts`）�?
- ?��??�路径校�?- `realpath` 归�???- RW ?�载?�满足?�条件�??��???+ ?�户确认�?- 黑�??��?绝�?`.ssh`?�`.aws`?�`.env`?�`.config/OC` 等�?

## 6. 审批�?
- **self-mod**（install_packages / add_mcp_server）�???HOLD，�? owner 审批；precheck 硬�?（�??�正???��?/payload 上�?�?- **CLI admin ?�令**：�? admin ?�户调用??hold ??建审????approval-pending ??host ?��??��???- **审批?�放**：�??�放?��?行�??�好一次�?；grant ?�带活�?复核
- **审批人�?好链**：scoped admins ??global admins ??owners

## 7. Agent ?��???
- **bash 工具**：只?�在容器?�执行�?超时 clamp 10 ?��?
- **?�件工具**：路�?resolve ?��?缀?��?，�??�在 `/workspace` ??- **ask_user 交�?**�?00s 超时 + `questionId` 精确等值匹??+ `kind` 专用 + 消费?��?
- **web_fetch**：�???`untrusted: true` ?�注；SSRF ?�护为基础容器网�??�离

## 8. 已知安全?��?

| �?| ?��? | ?��? |
|---|---|---|
| 密钥进容??env | 弱�??�线 OneCLI 网关 | ?�移植�?> ?�离强度（已记�?�?|
| Windows ?��?管�???0600 等价 ACL | 依�??�件系�??��? | 仅平?�支?��?Unix ??chmod 0600�?|
| `web_fetch` ?��??��? SSRF 过滤 | 依�?容器网�??�离 | 简?��???|
| SMTP 587 STARTTLS 验�?证书 | 已�???| 修�?记�?�?commit history |