# OC ?¶æ?è®¾è®¡

> è¯´æ? OC ?„æ ¸å¿ƒæ¶?„è®¾è®¡ï??•è?ç¨‹ä¸»?ºç??’ã€å? DB ä¼šè?æ¨¡å??è¯·æ±‚æ??å®¹?¨é?ç¦»ã€æ¨¡?—å??©å???
## 1. ä¸€?¥è??¶æ?

?•è?ç¨?Node ä¸»æœºç¼–æ??Œæ?ä¼šè?ä¸€ä¸?Docker å®¹å™¨?ç? Agent ?†ç¾¤?‚ä¸»?ºä?å®¹å™¨ä¹‹é—´**æ²¡æ? IPC**ï¼Œå”¯ä¸€ IO ?¢æ˜¯æ¯ä?è¯ä¸¤??SQLite?”â€”`inbound.db`ï¼ˆä¸»?ºå?/å®¹å™¨?ªè¯»ï¼‰å? `outbound.db`ï¼ˆå®¹?¨å?/ä¸»æœº?ªè¯»ï¼‰ï??„æ?ä»¶æ°å¥½ä?ä¸ªå??…ã€?
## 2. å®ä?æ¨¡å?

```
users (id "<channel>:<handle>", kind, display_name)
  ?”â? user_roles (user_id, role, agent_group_id) ??owner | admin
  ?”â? agent_group_members (user_id, agent_group_id) ??unprivileged access gate

agent_groups (id, name, folder, agent_provider)
  ??messaging_group_agents (wiring: session_mode, engage_mode, priority)

messaging_groups (id, channel_type, platform_id, instance)
  ?”â? sessions (agent_group_id + messaging_group_id + thread_id)
```

## 3. ??DB ä¼šè?æ¨¡å?

æ¯ä¸ªä¼šè??‰ä¸¤ä¸?SQLite ?‡ä»¶ï¼Œç²¾ç¡®ä?ä¸ªå??…ï?

| ?‡ä»¶ | ?™è€?| è¯»è€?| ?…å®¹ |
|------|------|------|------|
| `inbound.db` | ä¸»æœº | å®¹å™¨ï¼ˆåªè¯»ï? | `messages_in`?`delivered`?`destinations`?`session_routing` |
| `outbound.db` | å®¹å™¨ | ä¸»æœºï¼ˆåªè¯»ï? | `messages_out`?`processing_ack`?`session_state`?`container_state` |

**ä¸‰æ¡?¿é?ä¸å???*ï¼?1. `journal_mode=DELETE` ??WAL ??mmap -shm ??VirtioFS ä¸Šä?è·¨æ?è½½ä???2. ä¸»æœºæ¯æ¬¡?ä? `open-write-CLOSE` ???¿è??¥ä??»ç?å®¹å™¨è§†å›¾
3. ä¸»æœº?¶æ•° `seq` / å®¹å™¨å¥‡æ•° `seq` ???Œè½¦?“é?ç¦»ï?æ°¸ä??²ç?

å¿ƒè·³?¯æ?ä»?touchï¼ˆ`/workspace/.heartbeat`ï¼‰ï?ä¸æ˜¯ DB ?™ã€?
## 4. è¯·æ?æµ?
```
?¨æˆ·æ¶ˆæ¯ ???šé??‚é??¨æ¥????Router è·¯ç”±
  ??messaging_group è§??ï¼ˆå¹³??ID ??ç¾¤ç?è¡Œï?
  ???‘é€è€…è§£?ï?upsert usersï¼?  ??wiring ?¹é?ï¼ˆmessaging group ??agent groupï¼?  ??è®¿é—®?¨æ§ï¼ˆpermissions guardï¼?  ??session è§??ï¼ˆæ? session_mode ?›å»º/å¤ç”¨ï¼?  ????messages_in ??inbound.db
  ??wakeContainer() ?¤é? Docker å®¹å™¨
  ??Agent Runner è½®è¯¢ inbound.db ???¼å?????Provider è°ƒç”¨ LLM
  ????messages_out ??outbound.db
  ??ä¸»æœº delivery è½®è¯¢ outbound ???šé??‚é??¨æ??????¨æˆ·
```

## 5. å®¹å™¨?”ç¦»

- Docker å®¹å™¨ï¼Œæ?ä¼šè??¬ç?ï¼š`--cap-drop=ALL --security-opt=no-new-privileges --init`
- ?‡ä»¶ç³»ç??”ç¦»ï¼šåª?‚è½½?¾å??‡å?è·¯å?ï¼ˆ`/workspace`?`/workspace/agent`?`inbound.db`?`outbound.db`ï¼?- èµ„æ??åˆ¶ï¼š`--cpus`?`--memory`?`--pids-limit`
- Agent è¿è???Bun è¿è??¶ï?**ä¸ä?èµ?tsc ç¼–è?**ï¼ˆ`bun run /app/src/index.ts`ï¼?- å¯†é’¥ç»?`--env-file`ï¼?600ï¼‰æ³¨?¥ï?**ä¸è? docker run argv**

## 6. æ¨¡å??–æ‰©å±?
?¸å???æ³¨å?è¡?+ ?©å?"æ¨¡å?ï¼?- **?šé??‚é???*ï¼šè‡ªæ³¨å?ï¼ˆ`registerChannelAdapter`ï¼‰ï?`onInbound` ?è?æ³¨å…¥
- **æ¨¡å?**ï¼š`permissions`/`approvals`/`scheduling`/`a2a`/`self-mod`/`memory-kb` ç»?barrel ?¯ä??¨æ³¨?Œé’©å­?- **Provider**ï¼š`registerProviderContainerConfig` æ³¨å?å®¹å™¨è´¡çŒ®ï¼ˆenv/?‚è½½ï¼?- **CLI ?½ä»¤**ï¼š`registerCommand` å£°æ?å¼æ³¨?Œï?scope ?§åˆ¶?ƒé?
- **?€??*ï¼š`nc:` ?‡ä»¤å¼•æ?ï¼ˆcopy/append/env-setï¼‰ï?å¹‚ç?å®‰è?

?¸å?ä»??ä¸ç›´?¥è??¨æ¨¡?—ï??šè?æ³¨å??„å?è°ƒã€‚æœªå®‰è?æ¨¡å??¶æ ¸å¿ƒé?çº§è?è¡Œã€?
## 7. ?ªä¸»?©å?ï¼ˆç›¸å¯?nanoclaw ?ºçº¿ï¼?
1. **Web ç®¡ç??§åˆ¶??+ React ?ç«¯**ï¼šREST API + SSE äº‹ä»¶?´æ’­ + 6 é¡µé¢ç®¡ç? UI + ä¸‰è¯­ i18n
2. **è¯„ä¼°ä¸è?æµ‹ä?ç³?*ï¼šæ?ç´¢æ???+ Judgeï¼ˆMock/Llmï¼? ?’ç?ç»Ÿè®¡ + è½¨è¿¹ JSONL
3. **RAGï¼ˆmemory-kbï¼?*ï¼šå??—æ?ç´?+ ?¯æ³¨??embedding ?‘é?æ£€ç´?+ `kb_search` ?¥å…¥ agent
4. **9 æ¸ é??…ç½®**ï¼šTelegram/Discord/Slack/é£ä¹¦/?‰é?/ä¼å¾®/Email/Webhook/CLI
5. **openai/ollama provider ?…ç½®** + mock provider ??key ç«¯åˆ°ç«?6. **20 å®¹å™¨?€??*
7. **æµå?å¢é??•é€?*ï¼šprovider æµå?è§?? + poll-loop ç¼–è?å¼å???+ telegram editMessageText