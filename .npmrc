# .npmrc —— 仅存放认证/registry 类配置（pnpm 11 起其余配置必须放 pnpm-workspace.yaml）
# 说明：
#   - registry 固定 npmjs 官方源：pnpm 11 的 minimumReleaseAge 门禁依赖 packument 的 time 字段，
#     npmmirror 简化元数据缺失 per-version time，导致旧版本被误判为"刚发布"（2026-08-12 实测）。
#   - 供应链策略（allowBuilds/minimumReleaseAge）见 pnpm-workspace.yaml。
# 修改记录：
#   2026-08-12 创建（阶段 0）
#   2026-08-12 移除 minimum-release-age（pnpm 11 不再从 .npmrc 读取，已迁入 pnpm-workspace.yaml）
#   2026-08-12 registry 固定为 npmjs（npmmirror 与 minimumReleaseAge 门禁不兼容）
registry=https://registry.npmjs.org/
