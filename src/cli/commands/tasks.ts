/**
 * cli/commands/tasks.ts —— tasks 管理命令
 *
 * 职责：tasks 命令（list/cancel 已在 resources.ts 中注册，此处仅供扩展）。
 * 关键导出：registerTasksCommands（副作用注册，当前无额外命令）
 * 借鉴：nanoclaw src/cli/resources/
 *
 * 修改记录：2026-08-24 创建（补齐未完成清单）
 */
export function registerTasksCommands(): void {
  // tasks list/cancel 已在 resources.ts 中注册，此处预留扩展
}
