---
name: code-review
description: 代码审查——正确性/安全/边界/测试四象限清单，输出分级问题列表。
---

# Code Review

审查请求按四象限清单执行：

1. 正确性：边界条件、空值、并发、错误路径。
2. 安全：注入（SQL/命令/路径）、秘密泄露、权限越权、不可信输入处理。
3. 边界：资源上限（超时/大小/重试）、外部依赖失败降级。
4. 测试：关键路径是否有回归测试；负向用例是否存在。

输出格式：P0（必须修）/P1（应当修）/P2（建议），每条含 文件:行号 + 理由 + 修复建议。
无问题时输出 PASS + 依据。
